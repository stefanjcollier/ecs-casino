<style>
    body {
        background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 99%, #fad0c4 100%);
        color: #fff;
        font-family: 'Arial', sans-serif;
        text-align: center;
        padding: 20px;
    }
    .celebration {
        margin-top: 100px;
        animation: pop 1s ease-in-out infinite alternate;
    }
    .celebration h1 {
        font-size: 4rem;
        text-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }
    .celebration h2 {
        font-size: 2.5rem;
        margin-bottom: 20px;
    }
    .confetti {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 0;
    }
    @keyframes pop {
        from {
            transform: scale(1);
        }
        to {
            transform: scale(1.05);
        }
    }
</style>
<div class="celebration">
    <h1>
      🎉 Congratulations to our winners 🎉
    </h1>
    <h2>🥇 1st - <%= @winner_1.name %> ($<%= @winner_1.cash %>) </h2>
    <h2> 🥈 2nd - <%= @winner_2.name %> ($<%= @winner_2.cash %>)</h2>
    <h2> 🥉 3rd - <%= @winner_3.name %> ($<%= @winner_3.cash %>)</h2>
</div>

<!-- Confetti -->
<div class="confetti"></div>

<!-- Confetti Script -->
<script>
    // Confetti creation
    const confettiContainer = document.querySelector('.confetti');
    const colors = ['#ff7eb3', '#ff9a9e', '#fad0c4', '#fbc2eb', '#a18cd1'];

    for (let i = 0; i < 100; i++) {
        const confetti = document.createElement('div');
        confetti.style.position = 'absolute';
        confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        confetti.style.width = `${Math.random() * 8 + 4}px`;
        confetti.style.height = `${Math.random() * 8 + 4}px`;
        confetti.style.top = `${Math.random() * 100}vh`;
        confetti.style.left = `${Math.random() * 100}vw`;
        confetti.style.opacity = Math.random();
        confetti.style.transform = `rotate(${Math.random() * 360}deg)`;
        confetti.style.animation = `fall ${Math.random() * 5 + 3}s linear infinite`;

        confettiContainer.appendChild(confetti);
    }

    // Confetti falling animation
    const style = document.createElement('style');
    style.textContent = `
        @keyframes fall {
            to {
                transform: translateY(100vh) rotate(720deg);
            }
        }
    `;
    document.head.appendChild(style);
</script>
